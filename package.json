{
  "name": "video-service",
  "private": true,
  "version": "1.0.0",
  "packageManager": "yarn@1.22.22",
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "yarn workspace @video/api build && yarn workspace @video/web build",
    "test": "yarn workspace @video/api test",
    "db:generate": "yarn workspace @video/api db:generate",
    "db:bootstrap": "yarn workspace @video/api db:bootstrap",
    "db:seed": "yarn workspace @video/api db:seed"
  },
  "engines": {"node": ">=20"}
}
